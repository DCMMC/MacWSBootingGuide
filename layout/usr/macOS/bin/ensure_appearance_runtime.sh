# Compatibility wrapper retained for existing installations and operator
# muscle memory. The production invariant now covers every Ventura Settings
# extension, including Appearance.
bash /var/jb/usr/macOS/bin/ensure_settings_extensions_runtime.sh "$@"
